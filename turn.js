$("#flipbook").turn({
  // Aktifkan efek akselerasi
  acceleration: true,
  // Durasi animasi flip
  duration: 600,
  // Tambahkan gradien pada halaman
  gradients: true,
  // Tingkat elevasi halaman
  elevation: 5
});
